{
	"name": "mindustry-personal-computer-masm",
	"version": "1.0.0",
	"description": "",
	"type": "module",
	"scripts": {
		"start": "tsx ./src/index.ts",
		"prettier:fix": "prettier -w \"./**/*.{ts,json,md,prettierrc}\"",
		"prettier:check": "prettier -c \"./**/*.{ts,json,md,prettierrc}\"",
		"types:check": "tsc --noEmit",
		"check": "npm run prettier:check && npm run types:check",
		"fix": "npm run prettier:fix && npm run types:check",
		"clean": "rimraf ./dist/"
	},
	"author": "kennarddh",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.12.7",
		"prettier": "^3.2.5",
		"rimraf": "^5.0.7",
		"tsx": "^4.10.2",
		"typescript": "^5.4.5"
	}
}
